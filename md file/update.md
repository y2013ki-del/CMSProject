# UPDATE

이 파일은 현재 프로젝트 버전의 주요 변경 사항을 누적 기록하는 운영용 변경 이력 문서입니다.

## 기록 원칙

- 큰 기능 단위로 날짜별 섹션을 추가합니다.
- 실제 코드 반영이 끝난 뒤 요약만 남깁니다.
- 세부 설계는 `md file/` 하위 문서에 정리하고, 이 파일에는 “무엇이 바뀌었는지”만 남깁니다.
- 커밋 전에는 이번 작업이 `update.md`에 반영되었는지 확인합니다.
- 이미 문서화된 내용을 반복해서 길게 적지 말고, 변경된 범위만 추가합니다.

## 이력 확인 방법

```bash
git log --oneline -- "md file/update.md"
git diff HEAD~1..HEAD -- "md file/update.md"
git show HEAD -- "md file/update.md"
```

## 2026-05-04

### 콘텐츠 / 디자이너

- 콘텐츠 등록 흐름을 디자이너 중심으로 확장
- 일반 미디어도 디자이너 재편집 가능 구조 반영
- 웹 콘텐츠, 날씨 콘텐츠, 배치형 콘텐츠를 같은 디자이너 흐름 안에서 사용할 수 있게 정리
- `public/designer-content.html` 추가
- `public/weather-content.html` 추가

### 스케줄 / 채널 / 플레이어 반영

- 일반 파일에 디자이너 메타가 있으면 실제 송출 시 내부 디자이너 콘텐츠처럼 재생되도록 처리
- 콘텐츠 수정 후 `즉시 반영` 선택 시 전체 채널 재배포 가능
- 채널 재배포 API 추가

### 숨김 원본 / 콘텐츠 저장 안정성

- 디자이너 내부 원본 파일 숨김 상태를 조회 시점이 아니라 저장/수정/삭제 시점에만 재계산하도록 변경
- 서버 시작 시 1회 숨김 상태 재정리
- 디자이너 저장 실패 시 방금 업로드한 숨김 파일 롤백
- 디자인 콘텐츠 삭제 시 해당 콘텐츠 전용 원본 파일 정리 로직 추가

### 로그

- 관리자 변경성 작업 로그에 `actorIp` 기록
- 로그 탭 50줄 페이지 이동 추가
- 로그 다운로드 버튼 추가
- 기간 지정 다운로드 API 추가
- 로그 보존 기준은 30일 로테이션으로 문서화

### 문서

- `README.md` 최신 구조 기준으로 전면 갱신
- `md file/DOC_INDEX.md` 라우팅 보강
- `md file/PROJECT_STATUS.md` 현재 방향성 기준으로 재작성
- `md file/UI_GUIDE.md` 최신 UI 기준으로 재작성
- `md file/API_REFERENCE.md` 최신 API 기준으로 재작성
- `md file/OPERATIONS.md` 운영/데이터 이전 기준으로 재작성

### 확인 메모

- 현재 기준 운영 핵심 플로우:
  - 콘텐츠 등록
  - 스케줄 편집
  - 채널 규칙 설정
  - 기기 채널 연결
  - 플레이어 송출

## 2026-05-06

### 콘텐츠 / 디자이너

- 콘텐츠 등록 팝업에서 외부 파일 드래그앤드롭 업로드 지원
- 슬롯 직접 드롭 시 파일 추가와 배치를 한 번에 처리하도록 개선
- 등록된 디자이너/내부 웹 콘텐츠가 `LAYOUT` 텍스트 대신 실제 미리보기로 보이도록 수정
- 기기 모니터링 탭에서도 등록 기기 삭제 가능하도록 삭제 버튼 추가
- 콘텐츠 탭에 콘텐츠별 `음소거` 토글 추가
- 웹/레이아웃 기본 재생시간을 15초가 아니라 10초 기준으로 정리

### 스케줄 / 채널 편집

- 스케줄 아이템 기간 설정을 키보드 입력형(`YYYY-MM-DD HH:MM:SS`)으로 보강
- `빈 기간 = 자동 무한재생` 구조를 제거하고, `∞ 무한재생` 버튼을 눌러야만 무한재생되도록 변경
- 무한재생이 꺼진 상태에서 기간 미설정 항목은 경고 문구가 보이도록 정리
- 스케줄 삭제 시 현재 채널에서 사용 중이면 경고 문구를 함께 표시
- 스케줄 아이템 삭제, 채널 규칙 삭제, 디자이너 영역 삭제도 확인 팝업을 거치도록 통일

### 플레이어 반영

- 플레이어가 스케줄 아이템의 `alwaysOn` 값을 해석하도록 확장
- 무한재생 버튼을 끈 상태에서 기간이 비어 있는 항목은 자동 송출되지 않도록 정합성 맞춤
- 스케줄 아이템의 `muted` 값을 해석해 직접 재생되는 영상은 실제 음소거 적용

### 기기 제어 UI

- 일괄 볼륨 선택 UI의 `볼륨 n` 표기를 숫자만 보이도록 단순화
- `TV 채널 이동` 제어 항목과 관련 후속 입력 필드를 제거해 기기 제어 흐름 정리

## 2026-05-07

### 스케줄 / 채널 편집

- 스케줄 탭 편집 영역을 `등록 콘텐츠`와 `스케줄 구성` 2패널로 재배치해 좌-중-우 흐름이 보이도록 정리
- 스케줄 아이템에 `영상 음소거` 토글과 `콘텐츠 수정` 버튼 추가
- 스케줄 기간 입력에 텍스트 입력과 달력 버튼을 함께 제공하도록 수정
- 예약 규칙이 요일 규칙보다 항상 우선하도록 저장 우선순위와 미리보기 순서를 수정
- 요일 규칙을 기간 규칙으로 전환할 때 예약 탭으로 정상 이동하도록 섹션 전환 버그 수정
- 채널 규칙 편집/달력 상세에서 `규칙명` 입력을 제거하고 `예약 · 스케줄`, `요일 · 스케줄` 중심으로 정리
- 스케줄 아이템은 기간이 비어 있으면 자동 무한재생으로 해석하도록 UI 표시를 다시 정리

### 콘텐츠 등록 / 팝업 동작

- 스케줄 탭 등록 콘텐츠 카드에서도 바로 콘텐츠 수정 가능하게 연결
- 팝업 공통 모달에서 `Enter = 확인`, `Esc = 취소` 동작 추가
- 콘텐츠 등록 팝업에서 드래그 후 팝업 바깥에서 마우스를 놓아도 팝업이 닫히지 않도록 보강
- 저장 직후 그룹 1개를 고르는 흐름은 다시 제거하고, 콘텐츠 저장과 그룹 배정을 분리하는 방향으로 정리 시작
- 콘텐츠 탭 `전체 파일` 영역과 스케줄 탭 `등록 콘텐츠 / 스케줄 구성` 박스 높이를 키워 화면 하단 공간을 더 넓게 사용하도록 조정
- 콘텐츠 탭에서 `파일 업로드`와 `콘텐츠 등록` 흐름을 분리하고, 배치형 콘텐츠가 `WEB`처럼 보이지 않도록 표시 로직 정리
- 등록 모달, 콘텐츠 등록, 채널 캘린더 계열 팝업은 배경 클릭으로 닫히지 않도록 변경
- 스케줄 구성의 웹/배치형 콘텐츠도 `WEB` 텍스트 대신 실제 미리보기로 표시되도록 수정

### 기기 등록 / 제어

- 기기 등록을 `사이니지 / PC` 유형 분기로 확장
- `PC` 유형은 등록 시 MAC 주소와 재생 프로필 입력을 숨기고, 모니터링/제어 카드에서도 MAC, DUID, 재생 프로필, 전원 계열 제어를 제거

### 웰컴보드용 PC 플레이어 분리 방향

- CMS는 관리 기능을 유지하고, 기본 16:9 PC 재생은 웹 플레이어 구조를 유지하는 방향으로 정리
- 웰컴보드처럼 고해상도/비정형 송출이 필요한 PC만 별도 플레이어 앱으로 분리하는 장기 방향을 문서화
- 우선 Electron/Chromium 기반 플레이어를 검토하고, 장기적으로 웰컴보드용 고해상도 출력 엔진 역할을 강화하는 구조로 정리
- 공통 플레이리스트/명령 스키마, 상태 보고, 에러 보고, 현장 PC 배포/자동 실행/업데이트 설계 필요 사항을 `PROJECT_STATUS.md`에 추가
- CMS/플레이어 간 재생 계약 v1을 `PLAYER_CONTRACT.md`로 분리해 WebSocket 메시지, 재생 아이템, heartbeat, 에러 코드 기준을 정리
- CMS 발신 메시지와 웹 플레이어 heartbeat에 `playerContractVersion: 1`을 추가
- `pc_web`, `welcome_board_pc` 기기 유형을 서버 정규화 기준에 추가하고, 기존 `pc` 값은 `pc_web`으로 호환 처리
- 웹 플레이어 heartbeat가 앱 유형/버전, 화면 크기, 출력 영역, 준비 상태를 보고하도록 확장
- `/client` heartbeat는 Tizen 환경이면 `signage`, 일반 브라우저면 `pc_web`으로 앱 유형을 보고
- 관리자 기기 카드에 플레이어 준비 상태, 현재 콘텐츠, 앱 정보, 마지막 에러 표시 추가
- 웰컴보드용 PC 등록 유형을 추가하고, 기기별 송출 화면 해상도(`output.canvas`) 저장/수정 지원
- 웰컴보드용 PC에 전송하는 스케줄 payload에 기기별 `output` 설정을 포함하도록 확장
- 웰컴보드 출력 설정 UI에 송출 화면 해상도 입력 기준 추가
- 기기 등록 팝업이 화면을 넘어가지 않도록 최대 높이/내부 스크롤을 적용하고, 헤더 드래그 이동과 `Esc` 닫기를 추가
- 웰컴보드 기기 등록 입력을 `송출 화면 해상도` 기준으로 단순화하고, 콘텐츠 위치/크기는 콘텐츠 등록에서 다루도록 정리
- `welcome-player/`에 Electron 기반 웰컴보드 플레이어 MVP 추가
- 웰컴보드 플레이어 MVP는 CMS 서버 주소/기기 ID 저장, WebSocket 연결, 스케줄 수신, `output.canvas`/`output.rect` 적용, heartbeat 보고를 지원
- 콘텐츠 등록 팝업 헤더에 `웰컴보드 전환` 버튼 추가
- 웰컴보드 콘텐츠 등록 모드에서는 비율 고정 버튼을 숨기고, 등록된 웰컴보드 PC의 송출 화면 해상도를 불러와 스테이지 크기로 적용 가능
- 웰컴보드 콘텐츠 저장을 위해 디자이너 stageSize 허용 범위를 16384px까지 확장
- 웰컴보드 콘텐츠 등록의 해상도 불러오기 기준을 기기 등록의 송출 화면 해상도(`output.canvas`)로 정리
- 웰컴보드 콘텐츠 저장 시 기기 `output.rect`를 덮어쓰던 로직 제거
- 웰컴보드 플레이어 Windows x64 설치형/무설치 exe 빌드 설정 추가
- `welcome-player/dist/`에 `CMS Welcome Board Player-0.1.0-win-x64-setup.exe`, `CMS Welcome Board Player-0.1.0-win-x64-portable.exe` 생성 확인

### 캘린더 / 레이아웃

- 채널 캘린더 모달을 더 넓고 크게 사용하도록 조정하고, 월/주 뷰가 화면을 더 꽉 채우도록 확장
- 전체 관리자 화면 최대 폭을 넓혀 주요 편집 화면이 더 넓게 보이도록 조정

## 2026-05-07 (2차)

### 웰컴보드 플레이어 — 다중 디스플레이 개선

- Electron 플레이어에 다중 디스플레이 지원 추가: `screen.getAllDisplays()` 기반으로 창 위치·크기 지정
- 설정 패널에 `출력 디스플레이` 셀렉트박스와 `화면 이동` 버튼 추가
- `Ctrl+→ / Ctrl+←`(이전·다음), `Ctrl+1~9`(번호 직접 이동) 키보드 단축키 추가
- 설정 패널 열기를 `Esc` 키와 우측 하단 ⚙ 아이콘 모두에서 가능하도록 보강
- `preload.js`에 `getDisplays`, `moveToDisplay` IPC 노출
- `main.js`에 `app:get-displays`, `app:move-to-display` IPC 핸들러 추가
- 화면 이동 시 전체화면 해제 → 이동 → 전체화면 재적용(300 ms 딜레이) 처리

### 웰컴보드 플레이어 — 웹 콘텐츠 리로드 방지

- `state.activeWebUrl` 추적으로 동일 URL 반복 시 iframe 리로드 없이 유지
- `hideAllMedia(keepWebIfSameUrl)` 파라미터 추가
- WebSocket `update` 수신 시 스케줄이 실제로 변경된 경우에만 재생을 재시작하도록 시그니처 비교 추가

### 콘텐츠 등록 — 웰컴보드 해상도 불러오기 UX 개선

- 웰컴보드가 1대만 등록되어 있어도 반드시 선택 팝업을 거치도록 변경 (기존: 1대면 즉시 적용)

### 기기 등록 — 웰컴보드 출력 설정 단순화

- 기기 등록 폼에서 콘텐츠 위치·크기(rect) 입력과 프리셋 버튼 제거
- 모니터 해상도(canvas W/H)만 입력하도록 단순화; rect는 콘텐츠 등록 단계에서 결정

### 콘텐츠 등록(디자이너) — 웰컴 모드 전면 개선

- 웰컴 모드 스테이지를 콘텐츠 영역(rect) 대신 전체 모니터 해상도(canvas)로 변경
- 콘텐츠 슬롯을 스테이지 위 자유롭게 드래그·리사이즈 가능하도록 개선
- 기기 등록 해상도는 송출 화면(canvas) 기준으로 유지하고, 콘텐츠 등록 저장 시 기기의 `output.rect`를 덮어쓰지 않도록 정리

### 레이아웃 프리셋 기능 추가

- "웰컴보드 PC 해상도 불러오기" 버튼 옆에 "프리셋 ▾" 드롭다운 추가
- 현재 canvas + 슬롯 위치를 프리셋으로 저장 / 불러오기 / 이름 변경 / 삭제 지원
- 프리셋 데이터는 서버 `/data/layout-presets.json`에 저장
- API: `GET/POST/PUT/DELETE /api/layout-presets` 추가

## 2026-05-08

### 웰컴보드 해상도 기준 정리

- 기기 등록의 웰컴보드 해상도는 PC가 출력할 전체 화면 해상도만 의미하도록 정리
- 웰컴보드 콘텐츠 등록의 "PC 해상도 불러오기"는 `output.rect`가 아니라 `output.canvas` 전체 해상도를 stageSize로 적용
- CMS 기기 카드의 웰컴보드 출력 정보는 콘텐츠 영역이 아닌 송출 화면 해상도로 표시
- 웰컴보드 PC 신규 등록 기본 예시 해상도를 3840x2160으로 변경하고, 기존 테스트 기기 등록 데이터 초기화
- GitHub Pages에서 관리자 UI 정적 미리보기에 바로 접근할 수 있도록 루트 `index.html`과 `.nojekyll` 추가
